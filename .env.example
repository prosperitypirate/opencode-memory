# Copy this file to .env and fill in your keys
# cp .env.example .env

# ── Extraction provider ────────────────────────────────────────────────────────
# "anthropic" — Claude Haiku 4.5 ($1.00/$5.00 per MTok) — default, most consistent (93.5% avg)
# "xai"       — Grok 4.1 Fast ($0.20/$0.50 per MTok) — fastest, higher variance
# "google"    — Gemini 3 Flash ($0.50/$3.00 per MTok) — native JSON mode
EXTRACTION_PROVIDER=anthropic

# Required: Anthropic API key — used for memory extraction (claude-haiku-4-5)
# Get from: https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=sk-ant-...

# Optional: xAI API key — required when EXTRACTION_PROVIDER=xai
XAI_API_KEY=

# Optional: Google API key — required when EXTRACTION_PROVIDER=google
# Get from: https://aistudio.google.com/apikey
GOOGLE_API_KEY=

# Required: Voyage AI key — used for code embeddings (voyage-code-3)
VOYAGE_API_KEY=pa-...
