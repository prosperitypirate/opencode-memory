{
  "name": "codexfi-testing",
  "version": "0.1.0",
  "private": true,
  "description": "Tests for the codexfi plugin — unit, integration, and E2E",
  "scripts": {
    "test": "bun test src/unit/ src/integration/",
    "test:unit": "bun test src/unit/",
    "test:integration": "bun test src/integration/",
    "test:e2e": "bun run src/e2e/runner.ts",
    "test:e2e:scenario": "bun run src/e2e/runner.ts --scenario"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.0.0"
  }
}
